#!ruby -w

words =
  File.read("/usr/share/dict/words").split("\n").map(&:chomp).reject {|w| w.length > 6}.shuffle.take(4)
joiner = ["-", "/", "+"].sample
endcap = ["&", "!", "$", "@", "*"].sample
puts "#{endcap}#{words.join(joiner).downcase}#{endcap}"
